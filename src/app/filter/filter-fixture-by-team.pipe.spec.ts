import { FilterFixtureByTeamPipe } from './filter-fixture-by-team.pipe';

describe('FilterFixtureByTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFixtureByTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
